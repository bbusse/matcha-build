# Build container for matcha
[matcha](https://github.com/emersion/matcha) - A simple read-only web interface for Git repositories 

## Usage
```
$ git clone https://github.com/bbusse/matcha-build
$ cd matcha-build
$ podman build -t matcha-build .
```
